<template>
    <div class="edu-course course-style-6" :class="extraClass">
        <div class="inner">
            <div class="thumbnail">
                <NuxtLink to="/course/course-details">
                    <img :src="'/images/course/course-type-08/' + course.image" :alt="course.alt">
                </NuxtLink>
                <div class="course-price price-round">${{ course.currentPrice }}</div>
            </div>
            <div class="content">
                <span class="course-level">{{ course.category }}</span>
                <h5 class="title">
                    <NuxtLink to="/course/course-details">{{ course.title }}</NuxtLink>
                </h5>
                <ul class="course-meta">
                    <li><i class="icon-24"></i>{{ course.lessons }} {{ course.lessons > 1 ? 'Lessons' : 'Lesson' }}</li>
                    <li><i class="icon-25"></i>{{ course.students }} {{ course.students ? 'Students' : 'Student' }}</li>
                </ul>
            </div>
        </div>
    </div>
</template>

<script>
    export default {
        props: ['extraClass', 'course']
    }
</script>